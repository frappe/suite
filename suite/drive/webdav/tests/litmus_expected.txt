# Tolerated litmus non-passes, one per line: <group>:<test>:<FAIL|WARNING> reason [issue]
# CI fails on any FAIL not listed here, and on lines that now pass (delete them).
#
# TICKET 25 (WebDAV write relink): every method litmus needs is now on the wire.
# PUT, MKCOL, DELETE, MOVE, COPY, LOCK, UNLOCK, and PROPPATCH answer from
# `Drive Node`, and OPTIONS advertises "DAV: 1, 2, 3" again, so the http, basic,
# copymove, props, and locks groups can all be attempted.
#
# This ledger still holds one entry. Ticket 25 made no litmus run: litmus needs
# a served site, and the ticket-25 work was done in a worktree with no site of
# its own. The ledger may only grow from a real run, so nothing was added on
# expectation. Run the suite on the site gate and ledger what fails there.
basic:delete_fragment:WARNING werkzeug strips URI fragments before the app sees them, so a fragment-bearing DELETE cannot be told apart from a normal one
